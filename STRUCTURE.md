# AIX Landing - Структура проекта

## HTML Семантическая разметка

### Основная структура

```
<html>
├── <head> — метаданные
└── <body>
    ├── <header> (role="banner") — навигационный заголовок
    │   └── <nav> — основная навигация
    ├── <main> — основной контент
    │   ├── <section id="hero"> — герой / представление сервиса
    │   ├── <section class="partners"> — партнёры
    │   ├── <section id="benefits"> — преимущества для разных аудиторий
    │   ├── <section id="tools"> — все инструменты/функции
    │   ├── <section id="testimonials"> — отзывы пользователей
    │   └── <section id="pricing"> — тарифные планы
    └── <footer> (role="contentinfo") — колонтитул
        └── <nav> — дополнительная навигация
```

## Семантические теги по секциям

### 1. Header (Навигация)

```html
<header class="header" role="banner">
  <nav class="header__nav" aria-label="Навигация по сайту">
    <!-- ссылки навигации -->
  </nav>
</header>
```

**Атрибуты:**

- `role="banner"` — явно указывает роль интерактивного элемента
- `aria-label` — описывает назначение навигации для screen readers

### 2. Hero (Представление)

```html
<section class="hero" id="hero" aria-label="Представление AIX">
  <figure class="hero__video">
    <video>...</video>
    <!-- управление видео -->
  </figure>
</section>
```

**Семантика:**

- `<section>` — отдельная содержательная секция
- `<figure>` — обёрнуто видео как медиа-контент
- `id="hero"` — якорь для навигации

### 3. Benefits (Преимущества)

```html
<section class="benefits" id="benefits" aria-label="Преимущества сервиса">
  <article class="benefit-card">
    <!-- карточка с преимуществом -->
  </article>
</section>
```

**Семантика:**

- `<section>` — логическая область контента
- `<article>` — независимый, автономный контент (поддержка реиндексирования)

### 4. Tools (Инструменты)

```html
<section class="tools" id="tools" aria-label="Все инструменты AIX">
  <article class="tool-card" data-tool="analytics">
    <!-- карточка инструмента -->
  </article>
</section>
```

**Особенности:**

- `data-tool` — идентификатор для JavaScript обработки
- каждая карточка — `<article>` для независимого контента

### 5. Testimonials (Отзывы)

```html
<section
  class="testimonials"
  id="testimonials"
  aria-label="Отзывы пользователей"
>
  <figure class="phone-mockup">
    <figcaption class="phone-mockup__screen">
      <img alt="Отзыв пользователя 1" ... />
    </figcaption>
  </figure>
</section>
```

**Семантика:**

- `<figure>` — обёртка для визуального контента (макет телефона)
- `<figcaption>` — подпись к фигуре (содержимое экрана)

### 6. Pricing (Цены)

```html
<section class="pricing" id="pricing" aria-label="Тарифные планы">
  <article class="pricing-card">
    <!-- тариф -->
  </article>
</section>
```

**Структура:**

- `<article>` — каждый тарифный план как независимый элемент

### 7. Footer (Подвал)

```html
<footer class="footer" role="contentinfo">
  <nav class="footer__nav" aria-label="Информационные ссылки">
    <!-- ссылки -->
  </nav>
</footer>
```

**Атрибуты:**

- `role="contentinfo"` — явно указывает роль информационного блока

## SCSS Структура

### Архитектура файлов

```
sass/
├── style.scss              # 📌 Главный файл (импортирует все остальные)
├── _fonts.scss             # Подключение шрифтов
├── _variables.scss         # CSS переменные (цвета, размеры, переходы)
├── _reset.scss             # Сброс стилей браузера
├── _typography.scss        # Типографика (заголовки, текст)
├── _components.scss        # Переиспользуемые компоненты (кнопки, контейнер)
└── blocks/                 # 📁 Блоки для каждой секции
    ├── _header.scss        # Навигация
    ├── _hero.scss          # Герой секция
    ├── _partners.scss      # Партнёры
    ├── _benefits.scss      # Преимущества
    ├── _tools.scss         # Инструменты
    ├── _testimonials.scss  # Отзывы
    ├── _pricing.scss       # Тарифы
    ├── _footer.scss        # Подвал
    ├── _messages-intro.scss    # Страница сообщений (intro)
    ├── _messages-feature.scss  # Страница сообщений (features)
    └── _analytics.scss         # Страница аналитики
```

### Принципы организации

#### 1. **One Block = One File**

Каждый блок (секция) имеет свой SCSS файл в папке `blocks/`

```scss
// blocks/_hero.scss
.hero {
  // основной блок

  &__element {
    // элемент по БЕМ
  }

  &__element--modifier {
    // модификатор по БЕМ
  }
}
```

#### 2. **БЕМ Методология**

Все классы следуют БЕМ:

- **Block**: `.hero`, `.tools`, `.benefits`
- **Element**: `.hero__title`, `.tool-card__icon`
- **Modifier**: `.btn--primary`, `.tool-card--featured`

#### 3. **Иерархия стилей**

```scss
// Порядок определения в файле блока:

// 1. Основной блок
.block {
}

// 2. Элементы
.block__element {
}

// 3. Модификаторы блока
.block--modifier {
}

// 4. Специализированные элементы
.block__special-element {
}
.block__special-element__sub-element {
}
```

#### 4. **Переменные**

Все значения из `_variables.scss`:

```scss
// Цвета
--color-primary: #ff4900 --color-text: #ffffff --color-dark: #0e0e0e // Размер
  --container-max-width: 1140px --section-padding-y: 80px // Переходы
  --transition-fast: 0.2s ease --transition-base: 0.3s ease // Радиусы
  --radius-lg: 20px;
```

## Именование классов

### Компоненты

```
.header        — навигация
.hero          — герой-секция
.partners      — партнёры
.benefits      — преимущества
.benefit-card  — карточка преимущества
.tools         — инструменты
.tool-card     — карточка инструмента
.testimonials  — отзывы
.phone-mockup  — макет телефона
.pricing       — тарифы
.pricing-card  — карточка тарифа
.footer        — подвал
```

### Внутренние элементы (BEM)

```
.hero__title       — заголовок в герое
.hero__description — описание в герое
.hero__video       — видео в герое

.tool-card__icon   — иконка в карточке инструмента
.tool-card__title  — заголовок в карточке инструмента

.pricing-card__header  — заголовок карточки тарифа
.pricing-card__body    — тело карточки тарифа
```

### Модификаторы

```
.btn--primary      — основная кнопка
.tool-card--featured  — выделенная карточка инструмента
.pricing-card--free   — бесплатный тариф
```

## Доступность (Accessibility)

### ARIA атрибуты

```html
<!-- Для скрин-ридеров -->
<header role="banner">
  <footer role="contentinfo">
    <nav aria-label="Навигация по сайту">
      <!-- Для больших элементов -->
      <section aria-label="Представление AIX">
        <button aria-label="Включить звук"></button>
      </section>
    </nav>
  </footer>
</header>
```

### Семантические теги вместо div

- **❌ Неправильно:** `<div class="header">`
- **✅ Правильно:** `<header>` или `<header role="banner">`

- **❌ Неправильно:** `<div class="section">`
- **✅ Правильно:** `<section>`

- **❌ Неправильно:** `<div class="nav">`
- **✅ Правильно:** `<nav>`

## Принципы разработки

### 1. Mobile First

SCSS написан с mobile-first подходом:

```scss
.element {
  // мобильные стили (default)
}

@media (min-width: var(--breakpoint-md)) {
  .element {
    // планшетные стили
  }
}
```

### 2. CSS Variables (Custom Properties)

Все значения хранятся в переменных `:root`:

```scss
color: var(--color-primary)
font-family: var(--font-heading)
transition: var(--transition-base)
```

### 3. Glassmorphism эффект

```scss
-webkit-backdrop-filter: blur(10.65px);
backdrop-filter: blur(10.65px);
background: rgba(11, 11, 11, 0.21);
```

### 4. Префиксы для совместимости

```scss
/* WebKit (Chrome, Safari) */
-webkit-backdrop-filter: blur();
-webkit-filter: blur();

/* Стандарт */
backdrop-filter: blur();
filter: blur();
```

## Компиляция SCSS

### Production (минифицированный)

```bash
npx sass sass/style.scss css/style.css --style=compressed
```

### Development (расширенный)

```bash
npx sass sass/style.scss css/style.dev.css --style=expanded
```

### Watch mode

```bash
npx sass --watch sass/style.scss:css/style.css
```

## Ссылки и якоря

### Навигация к секциям

```html
<!-- Ссылка -->
<a href="#tools">Все инструменты</a>

<!-- Якорь -->
<section id="tools">...</section>
```

**Доступные секции:**

- `#hero` — представление
- `#benefits` — преимущества
- `#tools` — инструменты
- `#testimonials` — отзывы
- `#pricing` — цены

## Тестирование семантики

### Использованные инструменты

1. **Wave** (WebAIM) — проверка доступности
2. **Lighthouse** — аудит производительности и доступности
3. **Axe DevTools** — автоматизированные тесты доступности
4. **Screen Reader** (NVDA/JAWS) — ручное тестирование

### Что проверять

- ✅ Правильная иерархия заголовков (h1, h2, h3)
- ✅ Наличие alt текста для изображений
- ✅ ARIA labels для интерактивных элементов
- ✅ Правильное использование семантических тегов
- ✅ Контрастность текста (4.5:1 минимум)
